process myanalysis =  {
	untracked PSet maxEvents = {untracked int32 input = -1}

	source = PoolSource {
        	untracked vstring fileNames = {"file:Exhume_Hbb.root"}
	}
	
	service = TFileService { 
		string fileName = "analysis_histos.root"
  	}

	module myanalysis = ExhumeAnalyzer {}

	path p = {myanalysis}
}

